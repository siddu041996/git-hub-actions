name: android

on : [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: python command
        run: |
          import platform
          print
          (platform.processor())
          shell: python
  run-windows-commands:
    runs-on: ubuntu-latest
    steps:
      - name: directory powershell
        run: Get-Location
      - name: Dirctory Bash
        run: pwd
        shell: bash        